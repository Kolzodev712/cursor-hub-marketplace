---
description: "JS/TS testing: unit vs integration, deterministic, mocks, Jest/Vitest"
alwaysApply: false
globs:
  - "**/*.ts"
  - "**/*.tsx"
  - "**/*.js"
  - "**/*.jsx"
  - "**/tests/**/*"
  - "**/__tests__/**/*"
---

# JS/TS Testing

- **Unit vs integration:** Use unit tests for pure logic and single-module behavior; use integration tests for I/O or cross-module behavior. Keep tests fast and deterministic.
- **Deterministic tests:** Avoid reliance on wall clock, random (unless fixed seed), or external services. Use mocks or fixtures for I/O and timers.
- **Mocks:** Use the project's test runner (Jest, Vitest, etc.) for mocks and spies; avoid overspecifying implementation details.
- **Coverage:** Aim for meaningful coverage of behavior and edge cases, not "coverage theater." Prefer tests that would catch real regressions. Do **not** add tests that only assert what the implementation literally does with no independent specification.
- **Runner:** Use project conventions (`npm test`, Jest, Vitest, etc.). Respect project layout (e.g. `__tests__/`, `*.test.ts`).
